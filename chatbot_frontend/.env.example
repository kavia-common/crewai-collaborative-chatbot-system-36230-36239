# FRONTEND ENV VARS (do not commit .env with secrets)
# REQUIRED: Set the backend base URL (no trailing slash). Example: http://localhost:3001
REACT_APP_API_BASE_URL=http://localhost:3001
# OPTIONAL: WebSocket base (ws or wss). If not provided, derived from API base.
REACT_APP_WS_BASE_URL=ws://localhost:3001
# OPTIONAL: UI Theme
REACT_APP_THEME=light
